# Prompt 09: Phase 02 Admin Content Management

Work sequentially after Prompt 08 is complete and committed.

Implement only admin management updates for Phase 02 models and fields.

Required:

- Filament Resources/forms/tables for transcriptions where appropriate;
- category management;
- content tag management or approved Spatie plugin integration UI;
- item pinning controls;
- featured/main transcription controls;
- settings page/resource updates;
- resource URLs instead of hard-coded admin route names.

Do not implement public homepage/search, media extraction, dashboards, or studio features.

Tests must cover admin Resource smoke tests, create/edit validation, pin controls, featured transcription validation, category/tag management, and authorization expectations for authenticated admin access.

Run the project quality gate required by `AGENTS.md` before committing.
