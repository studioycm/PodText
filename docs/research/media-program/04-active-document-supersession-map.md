# Media Program Active Document and Supersession Map

## Active authority

1. Current operator request and approved Simplifier option.
2. `docs/phase-02/media-program-context.md`.
3. `00-media-program-requirements-decisions-and-method.md`.
4. `02-media-program-master-plan.md`.
5. Current package research/plan and committed handoff.
6. Current project state, ledger, source and tests.

## Superseded active route

The following audits/options are historical and must not be implemented:

- `LS-20260722-PODTEXT-MEDIA-ASSET-PROGRAM-06` /
  `MEDIA-CUTOVER-O1-DIRECT-ASSET-HYBRID-ROOT-MAINTENANCE`.
- `LS-20260722-PODTEXT-MEDIA-CUTOVER-LEAN-CORRECTION-07` /
  `MEDIA-CUTOVER-L1-ONE-COMMAND-IN-PLACE`.

Their trusted-only gallery, Needs Repair exclusion universe, byte
normalization/checksum proof, hybrid relocation, manifest/digest, quarantine,
conversion journal, schema-capability and association-clearing requirements are
rejected by the 2026-07-23 inventory-first reset.

## Historical evidence retained

Committed Curator G1, legacy-transition, media research and Gate 0 handoffs
remain historical evidence. Their bodies are not rewritten. When they conflict
with the active registry, the active registry wins.

The previously untracked
`docs/phase-02/media-program-p1-kernel-conversion-handoff.md` claimed completion
for the unfinished overbuilt draft. It is invalid/incomplete, was never a
committed handoff and is removed during exact cleanup. Package 1 closeout will
create a new handoff at that path from the implemented inventory-first work.

## Current package documents

- Package 1 pair and committed handoff: completed historical authority.
- Package 2 pair and committed handoff: completed historical authority.
- Package 3 acquisition pair and committed handoff: completed historical
  authority.
- `packages/03-post-acquisition-picker-ux-research.md`, plan and
  `../../phase-02/media-program-p3-post-acquisition-picker-ux-handoff.md`:
  completed historical authority under
  `LS-20260723-PODTEXT-MEDIA-P3-POST-ACQUISITION-UX-01` /
  `MEDIA-P3-POST-O3-IMMEDIATE-SOURCE-WORKSPACE`.
- Package 4 pair: forecast only; fresh audit required.
- Package 5 pair: forecast only; fresh audit required.

The earlier Package 4 audit
`LS-20260723-PODTEXT-MEDIA-P4-OWNER-IMAGE-UX-01` and options
`MEDIA-P4-O1-SHARED-DETAIL-MODAL` /
`MEDIA-P4-O2-SHARED-DETAIL-SLIDEOVER` are superseded and authorize nothing.
The post-Package-3 correction is closed locally. Package 4 must be freshly
audited and explicitly approved before implementation; no next package is
active.

## Related active track documents

`docs/phase-02/images-media-track-plan.md` keeps IMG-A/IMG-B/Curator history but
its forward route is this inventory-first program. Old root, normalization,
checksum, trusted-status and key-as-display language is historical only.

## Invalid interpretations

- A reference key is not proof that bytes are safe or eligible.
- A failed key/path/metadata/size/dimension rule does not remove inventory.
- A legacy path is not allowed to veto an authoritative attachment.
- A row without a MediaAsset binding still belongs in All Media until Package 1
  completes its bridge.
- Needs Repair does not mean hidden.
- Database conversion does not authorize file mutation.
- Ordinary correctness checks are not a dedicated security-audit phase.
